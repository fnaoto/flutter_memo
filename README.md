# flutter_memo
A memo app with flutter.

# launch emulator

```
$ flutter emulators
$ flutter emulators --launch <emulator id>
```

# run app

```
$ flutter run
```

# clean old dependencies

```
$ flutter clean
```
